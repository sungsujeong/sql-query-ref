Refer to a SQL problem: https://www.hackerrank.com/challenges/placements/problem?isFullScreen=true

Solution:
SELECT s.Name
FROM Students AS s 
    INNER JOIN Packages AS p ON s.ID = p.ID
    INNER JOIN Friends AS f ON s.ID = f.ID
    INNER JOIN Packages AS p1 ON f.Friend_ID = p1.ID
WHERE p.Salary < p1.Salary
ORDER BY p1.Salary;
